# Windows 桌面 WhatsApp 翻译软件依赖
# Python >= 3.9

# UI Automation - 自动读取 WhatsApp 窗口消息（不截屏）
uiautomation>=2.0.18
comtypes>=1.2.0

# 翻译后端
deep-translator>=1.11.4
requests>=2.31.0

# GUI
# tkinter 为 Python 内置，无需安装
pystray>=0.19.5
Pillow>=10.0.0

# 全局热键
keyboard>=0.13.5

# 配置持久化
pyyaml>=6.0.1

# 截图翻译 - 快速截屏
mss>=9.0.1

# Windows 内置 OCR 调用（Win10/11 系统自带，无需额外安装）
# 用于截图翻译和消息自动识别兜底
winsdk>=1.0.0b10

# 系统进程/窗口管理（窗口定位用）
psutil>=5.9.0

# Tesseract OCR（可选，如果系统安装了 Tesseract 也可以用）
# pytesseract>=0.3.10
