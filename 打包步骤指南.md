# 👣 从零开始打包 exe — 详细图文步骤

> 你现在的页面：一个空的 GitHub 仓库（`eurfariot2088-sudo/whatsapp-translator`）
> 目标：上传代码 → 触发打包 → 下载 exe

---

## 📌 第 1 步：上传所有代码文件到仓库

在你当前这个页面上，找到蓝色区域里的 **`uploading an existing file`** 链接，点它：

```
┌──────────────────────────────────────────────────────────────────┐
│  Quick setup — if you've done this kind of thing before         │
│                                                                  │
│  ......   creating a new file or uploading an existing file. .....│
│                  ^^^^^^^^^^^^^^^^^^^^^^^^  ← 点这个              │
└──────────────────────────────────────────────────────────────────┘
```

点进去后你会看到「Upload files」页面：

```
┌──────────────────────────────────────────────────────────────────┐
│  📁 Drag files here to add them to your repository              │
│                                                                  │
│         ┌──────────────────────────────────────┐                │
│         │   choose your files                  │                │
│         └──────────────────────────────────────┘                │
│                                                                  │
│  Or drag & drop individual files here to add them               │
└──────────────────────────────────────────────────────────────────┘
```

### 操作方式：直接拖文件夹进去

1. 打开你本地的 `whatsapp-translator` 文件夹（里面有 `main.py`、`config.py` 等）
2. **全选**里面所有文件和文件夹（Ctrl+A），包括：
   - `main.py`
   - `config.py`
   - `translator.py`
   - `whatsapp_reader.py`
   - `gui.py`
   - `requirements.txt`
   - `README.md`
   - `WhatsAppTranslator.spec`
   - `build.bat`
   - `build_onefile.bat`
   - `.github` 文件夹（**这个一定要传！Actions 工作流在里面**）
3. 用鼠标**拖到浏览器**的上传区域
4. 等所有文件上传完（文件名前面变绿 ✅）
5. 拉到页面底部，在 **Commit changes** 区域：
   - 标题填：`Initial commit`（或随便写）
   - 选择 **Commit directly to the `main` branch**
6. 点绿色按钮 **Commit changes**

> ⚠️ **关键**：`.github` 文件夹一定要成功上传。上传完成后回到 Code 页面，你应该能在文件列表里看到 `.github/` 这一项。

---

## 📌 第 2 步：确认 Actions 已启用并触发打包

### 2.1 进入 Actions 页面

回到仓库首页，点顶部导航栏的 **Actions** 标签：

```
Code   Issues   Pull requests   ↓Actions↓   Projects   Wiki
                              ^^^^^^^  点这个
```

如果是第一次进 Actions，可能会看到这个提示页面：

```
┌──────────────────────────────────────────────────────────┐
│  Get started with GitHub Actions                        │
│                                                          │
│  Workflows in this repository are disabled by default.  │
│  [ I understand my workflows, go ahead and enable them ]│
│                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^           │
│                  点这个按钮启用                           │
└──────────────────────────────────────────────────────────┘
```

点 **「I understand my workflows, go ahead and enable them」** 按钮启用。

### 2.2 手动触发打包

启用后，左侧会列出工作流。点 **Build Windows EXE**：

```
┌──────────────┬──────────────────────────────────┐
│ All workflows│  Build Windows EXE               │
│──────────────│                                  │
│ ↓Build Win.. │  No workflow runs yet           │
│  EXE         │                                  │
└──────────────┴──────────────────────────────────┘
```

然后在右侧找 **Run workflow** 按钮（灰色下拉按钮）：

```
┌──────────────────────────────────────────────────┐
│  Build Windows EXE          [↓ Run workflow ▾]  │
│                                       点这个      │
└──────────────────────────────────────────────────┘
```

点它会展开一个小面板：

```
┌─────────────────────────────────────┐
│ Run workflow                        │
│ Use workflow from:  main ▾          │
│                                     │
│   [ Run workflow ]   ← 绿色按钮，点  │
└─────────────────────────────────────┘
```

保持默认 `main` 分支，点绿色的 **Run workflow**。

### 2.3 等待构建

触发后你会在列表里看到一条橙色 ⏳ 的运行记录，标题是「手动触发」或 `Workflow dispatch`：

```
┌──────────────────────────────────────────────────┐
│ ⏳ Workflow dispatch   — build  — 刚刚           │
└──────────────────────────────────────────────────┘
```

点进去能看到实时进度，大概 **2~3 分钟**完成。
- 橙色 ⏳ = 正在运行
- 绿色 ✅ = 成功
- 红色 ❌ = 失败（失败了告诉我日志我帮你看）

---

## 📌 第 3 步：下载 exe

构建成功后（变绿 ✅），点进这次运行的详情页：

```
┌──────────────────────────────────────────────────┐
│ ✅ Build Windows EXE   #1   刚刚  2m 32s        │
│                                                   │
│  📋 Summary                                       │
│  🔧 build  (2m 30s)                               │
│                                                   │
│                    ... 往下滚 ...                 │
│                                                   │
│  ┌──────────────────────────────────────────┐    │
│  │  📦 Artifacts                            │    │
│  │                                          │    │
│  │  WhatsAppTranslator-exe     35 MB        │    │
│  │  ^^^^^^^^^^^^^^^^^^^^^                   │    │
│  │  ← 点这个就能下载 zip                      │    │
│  └──────────────────────────────────────────┘    │
└──────────────────────────────────────────────────┘
```

拉到页面最底部 **Artifacts（制品）** 区域 → 点 **WhatsAppTranslator-exe** → 浏览器开始下载一个 zip 文件。

解压这个 zip，里面就是 `WhatsAppTranslator.exe`，双击运行即可。

---

## 📌 第 4 步：运行 exe（可选小技巧）

1. 把 `WhatsAppTranslator.exe` 拷到任意 Windows 电脑
2. 双击运行
3. 会弹出主窗口，同时右下角系统托盘会出现一个小图标
4. 打开 WhatsApp Desktop，进入要翻译的聊天
5. 点「▶ 开始监听」即可

> 💡 首次运行 Windows 可能提示「SmartScreen 已阻止」—— 点 **更多信息 → 仍要运行** 就行。这是因为 exe 没有代码签名证书（签名证书一年要几千块，个人用不用签）。

---

## ❓ 常见问题排查

### Q1：上传后看不到 `.github` 文件夹？

在 GitHub 网页上 `.` 开头的文件夹是隐藏文件，默认是**能看到**的。如果你在文件列表里没找到 `.github/`，说明上传失败了。

**解决方法**：
- 用「creating a new file」的方式手动创建：
  1. 点 `creating a new file`
  2. 文件名填 `.github/workflows/build-exe.yml`（GitHub 会自动创建文件夹）
  3. 把 [build-exe.yml](../workflows/build-exe.yml) 的内容复制粘贴进去
  4. 底部点 Commit

### Q2：Actions 里看不到「Build Windows EXE」工作流？

说明 `.github/workflows/build-exe.yml` 文件没上传成功。回到第 1 步重新上传，或者用上面的方法手动创建。

### Q3：构建失败（红色 ❌）怎么办？

1. 点进失败的运行
2. 点红色的 `build` 步骤
3. 展开红色那一步的日志，截图或复制报错信息
4. 告诉我，我帮你修

### Q4：下载的 zip 解压后没有 exe？

不可能。确认下载的是 **Artifacts** 里的 `WhatsAppTranslator-exe`，而不是源代码 zip。

### Q5：免费额度够吗？

GitHub 免费账号每月 2000 分钟（Windows 按 2 倍算即 1000 分钟），每次构建约 3 分钟，够跑 300 多次，完全够用。

---

## 🚀 进阶：打 Tag 获取永久下载链接

Artifacts 只保留 90 天。想要**永久下载链接**可以打 Tag：

1. 仓库首页右侧点 **Create a new release**（或 Releases → Draft a new release）
2. **Choose a tag** → 填 `v1.0` → 点 `Create new tag: v1.0`
3. Release title 填 `v1.0`
4. 点 **Publish release**
5. 等 Actions 自动构建完，exe 会自动出现在 Release 页面的 Assets 里

以后打开 Release 页面就能直接下载，永久有效。

---

祝打包顺利！有任何步骤卡住了截图发给我。
